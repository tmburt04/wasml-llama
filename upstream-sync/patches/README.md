# patches

Ordered downstream deltas against upstream `llama.cpp` live here.

## Invariants
- One logical concern per patch file.
- Patch order is declared in `series.json`.
- Patch files must replay cleanly on top of the current upstream commit.
